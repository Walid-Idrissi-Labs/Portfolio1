"use client";

import { RevealLinks } from "./reveallinks-bit";

const DemoRevealLinks = () => {
  return (
    <div className="dark flex h-screen w-full items-center justify-center overflow-auto bg-black text-white relative">
      <RevealLinks />
    </div>
  );
};

export { DemoRevealLinks };